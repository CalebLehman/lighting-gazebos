# On Gazebos and Christmas Lights

For a friend who wasn't sure of the most efficient way to hang lights on
his gazebo.

Compile with `make` (requires **Asymptote** and a **LaTeX** distribution
with `pdflatex`).
